# 更新日志 (Changelog)

本项目所有重要变更都会记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v1.4.0] - 2026-09-19

> 本次发布的打包版本：**`QQAIbot_v1.4.0`**（包含 v1.3.1 以来的全部改动）。
> 打包目录：`QQAIbot_v1.4.0/QQAIbot/`（`QQAIbot.exe` + 插件 + 配置 + 说明）。

**本版要点**

1. 🛡️ **启动写缓存**：程序刚启动到第一次云同步完成之间，`data/` 下的数据改动先留在内存里，
   等云端数据拉下来之后再写盘 —— 解决"启动时的旧数据/默认值把云端数据覆盖掉"。
2. ⏱️ **同一条记录以时间戳更新的一方为准**：云端副本更新时不再上传本地旧副本，改用云端版本。
3. 📦 **启动缓存容量上限**（默认 8MB，含 128MB 兜底硬上限）+ 兜底超时，内存占用永远有界。
4. ✅ 第一次云同步完成时，控制台明确提示（恢复/上传/删除数量 + 用时）。
5. 🧹 一批内存与资源修复：回复限速表、繁忙提示线程、日志 handler、Ollama 会话表、
   后台读日志的内存尖峰、插件名单校验。

下面按改动顺序列出细节。

### 变更：同一条记录以时间戳更新的一方为准（不会再"本地旧数据覆盖云端新数据"）

- 上传阶段（C）新增判断：**云端是正常文件且比本地文件新 → 本轮不上传本地副本**，
  交给下载阶段（D）把云端的新版本拉回本地。
- 为什么重要：新机器 / `data/.cloud_sync_state.json` 丢失时，本地可能放着很旧的副本。
  原来会先把本地传上云（C 在 D 之前），于是**云端的较新数据被本地旧数据覆盖**。
- 发生这种情况时控制台会 WARNING 提示：
  `有 N 个文件的云端版本比本地新，本轮不上传本地副本、改用云端版本：xxx`（下个周期本地已是最新，不会重复提示）。
- 其它规则不变：本地更新 → 上传覆盖云端；时间戳相同 → 以本地为准；
  云端墓碑 + 本地更新 → 仍按"复活"处理；云端没有的本地新文件 → 正常上传。
- 状态页新增「云端较新(改用云端)」计数，退出时的累计统计也多了「云端较新改用云端 N 个」。
- 说明：判断依据是文件修改时间（毫秒级），请保证各机器系统时间准确（建议开启自动校时）。

### 新增：启动写缓存容量上限（`cloud_sync.startup_buffer_max_mb`，默认 8MB）

- 缓存内容超过上限时**立刻把已缓存的内容写回磁盘并停止缓存**，之后的改动直接写磁盘，
  避免机器人很忙时缓存无限占内存；控制台会 WARNING 说明原因。
- 即使把这个值填成 `0`（不限制），也有 **128MB 兜底硬上限**；
  把 `startup_buffer_minutes` 填成 `0`（不限等待时间）时也有 **30 分钟兜底超时**。
- Web 后台状态页的启动缓存提示会显示当前大小与上限（如「缓存中：3 个文件改动（12.4 KB / 8.0 MB）」）。

### 修复：内存与资源相关的若干问题（长期运行更稳）

- **日志显示挤成一行**（`core/web_admin.py`）：为省内存改成"从文件尾部按块回读"后，
  行内容里的换行符被 `splitlines()` 去掉了，而接口仍用 `''.join(...)` 拼接 →
  页面上日志全部挤成一行。现在每行都保留结尾换行符（与原来的 `readlines()` 语义一致），
  并把这个约定写进函数注释，避免以后再踩。
- **回复限速表**（`core/message_processor.py`）：`_last_reply_at` 原来按用户/群 key 只增不减，
  跑几个月、用户很多时会一直占内存；现在超过 2000 条时自动丢弃 300 秒前的记录。
- **队列满时的"繁忙提示"线程**：原来每条被拒消息都会新建一个线程，
  被刷屏时线程数可能瞬间暴涨；现在并发上限 5 个，超出的只记 WARNING 不回复。
- **日志 handler 泄漏**（`core/logger.py`）：重复创建日志器时原来只是 `handlers.clear()`（丢引用），
  现在是逐个 `close()` 后再清理，文件句柄与缓冲立即释放。
- **Ollama 插件会话记忆**（`plugins/ollama.py`）：`_histories` 原来每个用户永久留一份；
  现在用 `OrderedDict` 按最近使用淘汰，最多保留 200 个会话（可用环境变量
  `BOT_OLLAMA_MAX_SESSIONS` 调整，0=不限制）。
- **Web 后台读日志不再整份进内存**：状态页每 5 秒刷新一次，原来每次 `readlines()` 读完整份日志
  （单文件上限 10MB，会造成周期性内存尖峰）；现在从文件尾部按块回读（最多 4MB），
  按级别过滤时也会自动往前找够行数。
- **插件启用/禁用接口加白名单**：`/api/plugins/apply` 原来接受任意插件名（可被反复塞进禁用列表），
  现在只接受 `plugins/` 目录里真实存在的插件名，未知名字返回 400。
- 审计结论（供参考）：有界队列、转移码清理、统计 30 天裁剪、线程单例与退出 join、
  云同步状态表（随文件数、删除即清理）均正常；插件/上下文等按用户数增长的容器属正常数据规模。

### 提醒：Web 后台的安全配置

- 若 `web_admin.host` 填 `0.0.0.0`（手机/外网访问），**务必同时给 `web_admin.token` 填一个随机字符串**。
  token 留空时面板不做校验，同网络内任何人都能改配置、重载插件、触发同步。

### 新增：启动写缓存——刚启动时的文件改动不再覆盖云端数据

- 新模块 `core/deferred_writes.py`：从**程序启动**到**第一次云同步完成**这段时间里，
  对会被同步的数据文件（`data/` 下，不含日志、`config.json` 等不上云的文件）的改动
  （写入 / 追加 / 删除 / 改名）**先记在内存里**，等云端数据拉下来之后再统一写回磁盘。
- 为什么需要：插件与核心模块启动时常常立刻写数据文件（补默认值、整理格式、重置结构），
  而此刻本地数据可能比云端旧——这些写入一旦先落盘，云同步就会认为"本地更新"，
  **反而把云端数据覆盖掉**（换服务器/新机器最明显：本地是空的，一启动就写默认值）。
- 缓存期间的**读取会看到缓存里的内容**（`open()` / `os.path.exists` / `isfile` / `getsize` /
  `getmtime` 都按缓存回答），保证程序逻辑前后一致，不会读到旧数据。
- 实现方式：包装内置 `open()` 与 `os.remove/unlink/rename/replace/stat`、
  `os.path.exists/isfile/lexists`，所以**核心代码、插件、第三方库的写操作都会被拦下，插件不需要改任何代码**。
- 冲突处理（默认以云端为准）：某个文件刚被第一次同步从云端恢复（说明云端版本更新），
  就放弃启动期间对它的本地修改，并在控制台 WARNING 里列出文件名；
  想让本地修改优先，把 `cloud_sync.startup_buffer_keep_remote` 改成 `false`。
- **不会一直等**：等待超过 `cloud_sync.startup_buffer_minutes`（默认 3 分钟）、云同步进入暂停状态、
  程序退出时，缓存内容都会立即写回磁盘（绝不丢数据）。
- **没启用云同步（或凭据不完整）时完全不生效**：不缓存、不延迟落盘、也没有任何启动缓存提示，
  行为与没有这个功能时一模一样；**日志始终立即写盘**（含报错），不受影响。
- 新增配置：`cloud_sync.startup_buffer`（默认 true）、`startup_buffer_minutes`（默认 3，0=一直等）、
  `startup_buffer_keep_remote`（默认 true）；Web 后台「☁️ 云同步」分组可改，
  状态页在缓存期间显示「启动写缓存 ⏳ 缓存中：N 个文件改动还没落盘」。
- 云同步自己读写文件时（下载云端文件、写 `.cloud_sync_state.json`）会**临时挂起**缓存，
  必须看到磁盘真实状态；否则会把还留在内存里的启动期改动当成"本地已改动"上传。

### 新增：第一次云同步完成的控制台提示

- 第一次同步跑完时，控制台输出一条 INFO：
  `✅ 第一次同步已完成：从云端恢复 12 个文件，用时 3 秒——从现在起数据会与云端保持一致`；
  按实际发生的事拼装（上传 / 恢复 / 写入删除标记 / 本来就没有差异）。
- 本轮有文件失败时会补一句「另有 N 个文件本次失败，下个周期会自动重试」，
  并且**不再说"没有差异"**（避免误以为一切正常）。
- 连不上云端（同步抛异常）的那一轮不算完成：不提示，等真正同步成功的那一轮才提示；
  暂停中跳过的轮次同样不提示。状态页「⏳ 首次同步尚未执行」的提示与它同步消失。

### 修复

- **新配置项在后台显示成关闭/空值**：配置里没有的键（例如升级后新增的选项）在 Web 后台会显示为
  未勾选/空，用户一保存就把默认值写成了 `false`/`0`，功能被意外关掉。
  现在 `/api/config` 会先用 `DEFAULT_CONFIG` 补齐缺失项再交给页面，新选项显示的就是实际生效的默认值。
- **流程图文档同步更新**：`docs/云同步流程图.svg`、`docs/云同步流程图.md` 已按实际流程重画
  （启动阶段不再阻塞拉取、新增启动写缓存与"第一次同步已完成"节点）。

## [v1.3.1] - 2026-09-12

### 新增：后台支持下载指定日志 + 手机端适配 + 启动更快

- **下载指定日志**：日志页新增「日志文件」下拉（按时间倒序、显示大小）+「⬇️ 下载该日志」，
  以附件形式下载原始 `.txt`（新增接口 `GET /api/logs/list`、`GET /api/logs/download?name=`）。
  文件名做白名单校验（只允许 `data/logs/*.txt`），**目录穿越 / 非 txt / 不存在的文件一律 404**，token 校验照旧。
  为避免误解，该区域加了明确说明：下拉**只用于下载、不会改变下方显示的内容**，
  并在预览上方标出「当前显示：最新日志文件 xxx（共 N 个）」。
- **手机端适配**：新增 `@media (max-width:820px)`、`(max-width:480px)` 两档响应式样式：
  导航横向滚动不换行、卡片/表格缩小内边距、状态网格改单列、输入框字号 ≥16px（避免 iOS 聚焦放大页面）、
  主按钮通栏、底部保存条竖排。
- **修复上下文页文件名超出方框**：长 openid 文件名允许任意位置换行，表格外层加横向滚动容器。
- **修复插件列表文字互相遮挡**：固定列宽下长文本会溢出压到下一列，现改为单元格内换行
  （状态/操作列保持不换行），并加横向滚动容器；列宽重新分配为 20/28/9/16/11/16。
- **启动更快 + 云同步延后到连接 QQ 之后**：启动阶段不再访问云端（实测初始化从"先拉一次云端"变为 0 次请求），
  真正的首次同步在**成功连接 QQ 后立即执行**（`CloudSync.start(immediate=True)`）；
  在首次同步完成前，控制台会输出 WARNING 提示"刚启动这段时间数据可能不是最新的"，
  状态页「☁️ 云同步」也会显示「⏳ 首次同步尚未执行」。

### 新增：删除墓碑（tombstone）——删除也能正确同步

- 删除某个上下文（如 `/clear`）时不再硬删云端行，而是写入一条**带时间戳的删除标记**
  （`key + deleted=1 + 删除时间`），同步阶段顺序改为
  **A 应用云端删除标记 → B 本地删除写墓碑 → C 上传（可复活）→ D 下载（跳过墓碑）→ E 清理过期墓碑**。
- 效果：
  - **新服务器/状态文件丢失**的机器不会再把已删除的数据拉回来（修复了旧方案的复活问题）；
  - **多台机器之间能正确传播删除**：本地副本比墓碑旧 → 同步删除本地副本（`apply_remote_deletes`，默认开）；
  - **删除后又改过**（本地比墓碑新）→ 保留并重新上传，覆盖墓碑（last-write-wins，数据不丢）；
  - 墓碑默认保留 30 天（`tombstone_days`，0=永久），过期自动清理。
- 表结构新增 `deleted` 列；**老库自动 `ALTER TABLE` 升级**（列已存在时忽略报错），无需手动改表。
- 状态页新增「上次删除标记 / 应用云端删除 / 本地更新复活」计数。
- 流程图文档已同步更新（`docs/云同步流程图.svg`、`docs/云同步流程图.md`）。

### 新增：云同步（Cloudflare D1）——换服务器不丢用户数据

- 新增 `core/cloud_sync.py`：把 `data/` 下的**用户数据**同步到 Cloudflare D1 云数据库，
  换服务器 / 重装系统后启动即可把数据拉回来。
  - 同步内容：`user_context/**`（私聊/群聊上下文 + `bindings.json` 身份绑定）、`stats.json`、
    `plugins_data/**`（插件数据）、`plugins_disabled.json`、`command_panel.json`
  - **永不上云**：`config.json`、`config配置说明文件.txt`（含 AppSecret / API Key）、
    `data/bot.lock`、`data/bot.pid`；日志默认也不上云（`upload_logs=true` 才同步）
- 工作机制：`pull_on_start` 启动时先拉取（在模块初始化之前完成，保证上下文立刻可用）→
  后台线程按 `interval_seconds` **定时写入**（默认 60 秒；设成 300 即每 5 分钟一次，
  间隔 ≥60 秒时对齐到整点倍数：300 秒就落在每小时 00/05/10… 分整）→ 退出前再做一次；
  每个周期只上传**有改动**的文件（无改动则完全不写库，省额度），多个改动会合并成一次批量写入。
  本地删除的文件（如 `/clear`）会同步删除云端记录，不会被拉回来。
- 后台「状态」页新增 **☁️ 云同步** 卡片：上次写入时间、上传/恢复/删除数量、写入间隔，
  并提供「⬆️ 立即同步一次」按钮（POST `/api/cloud_sync/sync`）。
- 安全与稳健：只同步文本文件、单文件超过 `max_file_mb` 自动跳过、网络异常只记 WARNING 下个周期重试、
  未配置完整时跳过并提示；同一时间建议只运行一台机器人（多台会互相覆盖）。
- 新增 `cloud_sync` 配置段与 Web 后台「☁️ 云同步」分区（API 令牌按敏感字段处理，留空=保持不变）。
- **流程图文档**：`docs/云同步流程图.svg`（图形版，浏览器打开）与 `docs/云同步流程图.md`
  （Mermaid 文本版，GitHub / VS Code 可直接渲染），说明启动拉取 → 定时写入 → 退出同步的完整逻辑。

### 新增：控制台彩色输出

- 控制台日志**时间显示灰色、WARNING 黄色、ERROR 红色、CRITICAL 亮红**，正文保持原色不着色
  （`core/logger.py`：`ColorConsoleFormatter`）。
- **日志文件始终为纯文本**（不带 ANSI 转义），避免用编辑器/后台日志页查看时出现乱码。
- 智能判断：只有输出到**终端**时才上色；重定向到文件/管道、或没有控制台（如 `-w` 打包的 exe）时自动降级为纯文本。
- Windows 会自动开启控制台的 ANSI（VT）支持；开启失败则不上色，不会输出乱码。
- 新增配置项 **`log.console_color`**（默认 true，日志页可改，改后需重启）；
  也支持环境变量 `NO_COLOR=1`（强制关闭）与 `FORCE_COLOR=1`（强制开启）。

### 新增：无需 API Key 也能运行

- `api_key` / `base_url` / `model` 未填时不再报 ERROR 阻止启动，改为 WARNING 并继续运行；
  只有 `qq.app_id` / `qq.app_secret` 仍是必填。适用于「插件接管回复」「只用关键词回复」「只用内置指令」的场景。
- `core/ai_client.py` 新增 `available`；未配置时不下载图片、不做无谓请求，只提示一次 WARNING。
- 新增配置项 **`no_ai_reply`**：没被插件接管的普通消息在「无 AI」时回这句；留空=完全不回复。

### 新增：内置 AI 总开关 `ai.enabled`

- **`ai.enabled=false`**：即使 `config.json` 里填了 API Key，内置 AI 也**完全不参与回复**，
  普通消息交给插件/关键词回复处理（没被接管的按 `no_ai_reply` 回复或保持安静）。
  适合"用本地 Ollama 插件接管回复、不想消耗线上额度"的场景；支持热更新，改完保存即生效。
- Web 后台「🧠 AI 服务配置」新增「启用内置 AI」开关与「未配置 AI 时的兜底回复」两项。
- 启动日志会明确提示当前状态：已关闭 / 未配置完整 / 使用哪个模型。

### 新增：Ollama 插件强制接管（`plugins/ollama.py` v2.3.0）

- **`FORCE_TAKEOVER`（默认 True，环境变量 `BOT_OLLAMA_FORCE`）**：
  - `true`：所有普通消息都由本地 Ollama 回答，**即使主程序填了 API Key**；
    插件出错也只返回错误提示，绝不让内置 AI 顶替回答。
  - `false`：主程序内置 AI 可用时插件主动让路（返回 `None` 交回内置 AI）；
    没配 AI 或 `ai.enabled=false` 时才接管（兜底模式）。
- `on_message` 全量异常兜底 + 空回复兜底，保证"接管"不会被异常/空值打断。
- 新增 `on_start` 日志说明当前接管方式。

### 新增：连续失败 3 次 → ERROR 并暂停同步

- 同一个文件（上传/下载/写入本地/删除本地/写删除标记）**连续失败 3 次**时：
  第 1~2 次记 WARNING（写明"第 n/3 次"），**第 3 次升级为 ERROR 并暂停云同步**，
  暂停期间完全不访问云端（省额度、也不刷日志）。
- 恢复方式：
  - **自动**：`cloud_sync.error_pause_minutes`（默认 30 分钟）到期后自动恢复并重置失败计数；
    填 `0` = 不自动恢复，只能手动；
  - **手动**：后台「状态 → ☁️ 云同步」点 **「⬆️ 立即同步一次」**（强制重试，成功即恢复）
    或 **「▶️ 解除暂停」**。
- 状态页在暂停时会显示：⛔ 已暂停、暂停原因、恢复方式（倒计时/需手动）、连续失败的文件列表。
- 批量上传失败按批内文件计数（避免逐文件刷屏），轮内计数在成功一次后清零，不会"累计误暂停"。

### 修复

- **热更新日志"无变化"让人困惑**：Web 后台保存时会立刻热应用一次，文件监视线程随后又发现
  `config.json` 的 mtime 变了、再应用一次（此时已无差异），于是出现"配置已热更新: 无变化"。
  现在：
  - `_apply_hot_config()` 结束时把当前 mtime 记为**已消费**，同一次修改不会被重复应用；
  - **没有实际变化时只写日志文件（DEBUG）**，控制台不再出现"无变化"；
  - 热更新日志会**标明来源**：`配置已热更新（来源：Web后台）: 云同步` / `（来源：文件监视）`，
    方便区分是谁触发的。
- **多实例检测误报（重启后以为自己还在跑）**：心跳记录 key 里带 pid，而停止时没有删除，
  于是重启后 pid 变了，新进程把**自己上一次运行留下的记录**当成"另一个实例"告警。现在：
  - 停止时删除自己的心跳（在最后一次同步之后删，避免又被写回）；
  - 检测时对**同一台机器**的记录做**进程存活检查**：pid 已退出 → 判定为上次运行/崩溃的残留，
    **静默清理且不告警**；pid 还活着才是真的多实例（照常 WARNING 告警，并附上对方的数据目录）；
  - 其它机器的心跳无法判活，仍按心跳时效（3 个周期）判断，过期记录由 GC 清理。
- **云同步日志刷屏**：删/恢复多个文件时，逐个文件名打到控制台会刷屏。现在
  **控制台只在程序退出时输出一行累计统计**（上传/恢复/删除标记/删除本地/复活/失败次数），
  每个周期的汇总与每个文件的明细**全部降为 DEBUG，只写日志文件**
  （后台「日志」页把级别切到 DEBUG 即可查看）。无改动的周期不写任何东西。
- **云同步失败可见性**：上传失败、下载失败、写入本地失败、删除本地失败、写删除标记失败、
  连接失败/超时/令牌错误、多实例冲突等，都会在**控制台输出 WARNING**（黄色）并写明原因与重试策略；
  单个文件失败只跳过该文件、**不影响本轮其它文件**，下个周期自动重试。
- **云同步反复上传同一批文件**：判断"文件有没有改动"时，一边用**解码后内容长度**、另一边用
  **磁盘字节数**；Windows 文本模式会把 `\n` 写成 `\r\n`，两者永远不相等 →
  这些文件每个周期都被重新上传（日志里出现重复的"已上传 N 个文件到云端"）。
  现在统一以**磁盘字节数**为准；下载文件时用 `newline='\n'` 原样写入，与云端内容逐字节一致。
- **新增多实例检测**：每个周期写一条实例心跳（`system/instances/<主机>-<pid>`），
  若发现另一个实例在 3 个周期内也同步同一个数据库，日志会 WARNING 提示
  "多实例会互相覆盖（并出现同一批文件被重复上传），请只运行一台机器人"；
  超过 1 天的旧心跳记录会自动清理。
- **插件读到旧配置**：热更新后注入给插件的 `bot.config` 仍是启动时的旧字典，
  导致插件判断（如"主程序有没有配 AI"）不准确；现在热更新会刷新该引用。
- **`no_ai_reply` 未热更新**：改完保存后不生效，现已纳入热更新。
- **Web 后台插件「启用」点了没反应**：原逻辑把“启用”当成“取消修改”，对已停用的插件无法重新启用；
  现改为以服务器真实状态为基准判断，并新增「待启用 / 待停用」徽章、整行浅黄标记、待保存提示与按钮计数。
- **待保存标记被「启用/停用」按钮遮挡**：状态列改为只显示一个徽章，并加宽到 11%。

## [v1.3.0] - 2026-08-23

### 自 v1.2.0 的重要更新

本版本在 v1.2.0 基础上，新增跨平台单实例保护、日志脱敏与 Markdown 清理等能力。

#### ✨ 新增（第一部分）

- **单实例保护（跨平台三级锁）**（`qqbot.py`）
  - 文件锁（`fcntl` / `msvcrt`）→ 目录锁（原子 `mkdir`，兼容 Android 共享存储 FUSE）→ 均不可用时提示后继续启动
  - 被占用时拒绝启动，提示按平台显示（Windows：`taskkill /F /PID`；手机/Linux：`pkill -f qqbot.py`）
  - 进程退出/崩溃自动释放锁；目录锁带残留检测（锁主进程已死则自动清理）
- **转移码日志脱敏**（`core/logger.py`、`core/qq_client.py`、`core/message_processor.py`）：新增 `mask_transfer_code()`，日志中的转移码显示为 `******`，不再明文落盘
- **转移码防堆积**（`core/context_manager.py`）：生成新码时自动作废该用户旧码，同一用户同时最多一个有效码
- **AI 回复 Markdown 清理**（`core/ai_client.py`、`core/message_processor.py`）：新增 `strip_markdown()`，去除加粗/标题/列表/代码块/链接/表格等格式符号；表格转为可读文本（`a | b`）并删除分隔行

#### ✨ 新增（第二部分）

- **QQ 指令面板（聊天界面指令列表）**（`core/qq_client.py`、`core/web_admin.py`、`qqbot.py`）
  - 接入官方 `v2/panels` 接口：创建 / 更新 / 查询列表 / 删除指令面板，私聊(c2c)与群聊(group)面板分别注册
  - 修复列表查询：官方返回字段为 `records` 且需按 `scope` 分页查询，之前解析不到面板导致"看不到/删不掉、额度一直被占"的问题
  - 面板注册改为"先查官方列表、有则复用"：本地缓存丢失或面板失效时自动复用/重建，不再重复创建占额度；创建失败提示到后台清理旧面板
  - 启动时先注册面板再连接 WebSocket（此前顺序颠倒导致永远不执行）
- **Web 管理后台 UI 全面改版**（`core/web_admin.py`）
  - 现代化视觉：靛蓝渐变头部、毛玻璃吸顶导航、圆角卡片与阴影、渐变按钮、自定义滚动条、淡入动画
  - 日志页新增**日志级别筛选**（全部 / DEBUG / INFO / WARNING / ERROR / CRITICAL），后端按级别过滤
  - 新增 **📈 统计页**：今日统计（消息/私聊/群聊/AI调用/关键词/指令/过滤/回复等）、最近 7 天纯 CSS 柱状趋势图、历史累计
  - 修复状态栏"当前北京时间"快 8 小时的问题（`localtime` 误用改为 `gmtime` 读取 UTC+8）
  - 指令面板编辑器改为 QQ 官方格式：每行 = 类型（指令/链接）+ 名称 + 描述或链接，保存生成 `{"type":"command","name","desc"}` 或 `{"type":"link","name","link"}`
- **图片识别**（`core/qq_client.py`、`core/file_handler.py`）：兼容 QQ 富媒体 `media` 字段（此前只读 `attachments` 导致收不到图），`file_info` 自动换取下载链接，图片下载转 base64 交给多模态 AI 识别；纯图片消息自动补提示"请描述这张图片"
- **回复限速**（`core/message_processor.py`）：同一用户两次 AI 回复的最小间隔（默认 3 秒），防刷屏；`rate_limit.enabled` / `rate_limit.interval_seconds` 可配，支持热更新
- **敏感词过滤**（`core/message_processor.py`）：输入拦截（`block_input`）与输出打码（替换为 `***`）双向过滤；`sensitive_words.list` / `replacement` / `block_input` 可配，支持热更新
- **插件系统**（新增 `core/plugin_manager.py`，`plugins/` 目录）
  - 用户**无需改源代码**即可扩展：把插件放进 `plugins/` 目录（单文件 `.py` 或一个文件夹多文件），Web 后台一键重新加载
  - 插件接口 v1：`PLUGIN` 元信息 + `COMMANDS` / `KEYWORDS` / `match(msg)` 匹配 + `on_message(msg[, bot])` 处理
  - 支持**连接型/桥接型插件**：`bot` 上下文可主动发消息/读配置/记日志，`on_start(bot)` / `on_stop(bot)` 生命周期钩子（启动后台线程、监听外部服务等）
  - Web 后台 **🧩 插件页**：插件列表（名称/说明/类型/匹配规则/状态）、重新加载、启用/停用（**保存后生效**，状态持久化到 `data/plugins_disabled.json`，重启保持）
  - 插件目录不存在时自动创建；插件加载日志精简（逐条为 DEBUG，控制台只显示"加载完成：共 N 个（另有 N 个被禁用）"）
  - 附带示例：`示例插件.py`（回复型）、`多文件示例/`（多文件）、`桥接转发示例.py`（连接型）、`Ollama本地AI.py`
- **Ollama 本地 AI 插件**（`plugins/ollama.py`）：默认接管普通文字消息，调用本机 Ollama 回答，**不消耗 API 额度**；任何 `/` 开头的指令（内置或其他插件的）一律放行；每个用户保留最近 6 轮对话记忆
- **单文件整合版**（`qqbot_single.py`）：全部模块合并为单文件（保留注释），修复打包脚本误删函数体内局部 import 的问题

#### 🐛 修复

- 修复 Android 共享存储（FUSE）不支持 `flock` 导致手机端一直误判"已在运行"而无法启动的问题（目录锁兜底）
- 修复单实例拒绝提示与平台不匹配的问题（Windows 上显示 pkill 命令）
- 修复"转移码无效"提示不准确的问题（区分无效 / 已过期 / 已被新码覆盖）
- 修复指令面板订阅未授权 intent 位（`1<<31`）导致 WebSocket 鉴权失败、程序无法连接的问题（恢复默认 `1<<25|1<<26`，欢迎新成员功能因平台权限限制移除）
- 修复插件停用后从后台列表消失、无法重新启用的问题（列表改为同时展示已停用插件）
- 修复插件说明多行时"单文件/运行中"等徽章被撑成多行的问题（徽章 nowrap + 表格固定列宽）
- 修复 Web 面板插件页按钮引号嵌套导致点击报错的问题（改用 `data-` 属性传参）

## [v1.2.0] - 2026-08-16

### 自 v1.1.0 的重要更新

本版本在 v1.1.0 原始版本基础上，修复了若干会导致功能失效的问题，并新增上下文管理、队列限流等能力。

#### 🐛 修复

**核心连接 (`core/qq_client.py`)**

- 修复 WebSocket intents 订阅缺失导致**收不到私聊(C2C)消息**的问题：由仅订阅群@消息（`1<<25`）改为同时订阅单聊消息（`1<<26`）
- 修复鉴权失败时**无限重连**的问题：重连计数改为在 READY（鉴权成功）事件后重置，AppID/Secret 无效时最多重试 5 次即放弃
- 新增**心跳超时检测**：连续 3 个心跳周期未收到服务器任何响应即判定连接假死，主动断开以触发重连
- 修复 access_token 拼接在 WebSocket URL 中导致**凭据可能泄露**到代理/网关日志的问题，鉴权改由连接内 Identify 包完成
- 修复沙箱环境下 WebSocket 仍连接正式网关导致**沙箱无法使用**的问题，沙箱/正式环境的 REST 与 WebSocket 网关统一切换
- 简化断线重连的 RESUME 会话恢复逻辑：非主动断开且存在历史会话即尝试 RESUME，失败自动回退重新 IDENTIFY

**消息处理 (`core/message_processor.py`)**

- 修复 `max_queue_size` 配置无效的问题：消息队列改为**有界队列**，队列满时不再静默丢弃，而是回复"🤖 机器人正在处理其他消息，请稍后再试。"（独立线程发送，不阻塞消息接收）
- 修复频道私信回复调用错误接口的问题，改为走单聊消息接口
- 修复分段发送时最后一段之后多余等待 0.5 秒的问题
- 清理分段逻辑中的死代码

**消息过滤 (`core/message_filter.py`)**

- 修复逻辑矛盾导致**纯数字、单字符等无意义消息永远无法被过滤**的问题
- 修复单字符正则误伤单个中文字符（如"好"）的问题
- 扩充纯表情过滤的正则覆盖范围（支持主流 emoji 区块）

**AI 客户端 (`core/ai_client.py`)**

- 修复重试最后一次失败后仍空等指数退避时间的问题
- 修复"思考过程"过滤正则可能误删回复正文的问题

**文件处理 (`core/file_handler.py`)**

- 修复 URL 带查询参数（如 `a.jpg?sign=xxx`）时文件扩展名解析错误的问题

#### ✨ 新增

- **私聊/群聊上下文分文件夹存储**：`data/user_context/private/{user_openid}.json` 与 `data/user_context/group/{群ID}_{用户ID}.json`，群聊上下文按群隔离，`/clear` 只清当前作用域
- **私聊上下文转移到群聊**：群内发送 `/转移私聊到群聊` 即可把私聊历史合并进当前群上下文——保留群聊原有内容、仅追加群聊中不存在的私聊记录（按内容去重，重复转移不产生重复记录），私聊上下文本身保留
- **身份绑定兜底机制**：私聊 `/生成转移码` → 群聊 `/绑定转移码 <码>`，用于平台 openid 命名空间不一致的场景（当前已实测 `member_openid` 与 `user_openid` 值相同，通常无需绑定）
- 群聊事件优先读取 `author.user_openid` 作为统一身份标识，兼容平台行为变化（取不到时回退 `member_openid`）

#### 🗑️ 移除

- 移除群聊回复中手动添加的 @用户 前缀，并删除相关配置项 `reply_with_mention`（`config.json`、配置说明、默认配置模板同步清理）

#### ⚠️ 已知限制

- `require_mention: false`（回复所有群消息）需要 QQ 开放平台开通"群消息全量"权限并额外订阅事件，当前未实现
- 群聊**被动回复**会被 QQ 平台自动 @ 原消息发送者，属官方服务端行为，无法通过代码或参数关闭

## [v1.1.0] - 原始版本基线

- 初始内部版本：基本 QQ 机器人功能（群@/私聊回复、上下文存储、关键词过滤、多模态图片、日志与配置管理）
- 本仓库首次公开发布即从该版本更新而来，变更明细见上方 [v1.2.0]
