# Release Notes — v1.3.0

QQ AI Bot 在 v1.2.0 基础上的更新版本。新增跨平台单实例保护、日志脱敏与 Markdown 清理等能力。

## ✨ 新功能

- **单实例保护（跨平台）**：同一份程序被重复启动时自动拒绝，避免重复回复与上下文冲突
  - 三级锁机制：文件锁（fcntl / msvcrt）→ 目录锁（原子 mkdir，兼容 Android 共享存储）→ 均不可用时提示后继续
  - 进程退出/崩溃自动释放锁，无残留；拒绝提示按平台显示对应命令（Windows 用 `taskkill`，手机/Linux 用 `pkill`）
- **转移码日志脱敏**：生成的 6 位转移码不再明文写入日志（显示为 `******`）
- **转移码防堆积**：同一用户生成新码时自动作废旧码，同一时刻每用户最多一个有效码
- **AI 回复 Markdown 清理**：自动去除 AI 输出中的加粗、标题、列表、代码块、表格等格式符号，QQ 里显示更干净
  - 表格自动转换为可读文本（`a | b`），不再满屏竖线横杠

## 🐛 修复 / 改进

- ✅ 修复 Android 共享存储（FUSE）不支持文件锁导致**手机端无法启动**的问题（目录锁兜底）
- ✅ 修复单实例拒绝提示与平台不匹配（Windows 上显示 pkill 命令）的问题
- ✅ 修复"转移码无效"提示不准确的问题（区分无效 / 已过期 / 已被新码覆盖）

## ✨ 新功能（追加部分）

- **QQ 指令面板**：接入官方 `v2/panels` 接口，私聊/群聊面板分别注册；后台「指令面板管理」可刷新列表、删除旧面板、一键重新注册；面板注册先查官方列表、有则复用，避免重复创建占额度
- **Web 管理后台全面改版**：现代化 UI（渐变/毛玻璃/圆角卡片）；日志页新增**级别筛选**（DEBUG/INFO/WARNING/ERROR/CRITICAL）；新增 **📈 统计页**（今日统计、7 天趋势柱状图、历史累计）；修复状态栏"北京时间"快 8 小时的问题
- **图片识别**：兼容 QQ 富媒体 `media` 字段，发图自动交给多模态 AI 描述/分析（纯图片消息自动补提示）
- **语音识别**：收到语音自动转文字后交给 AI（需在后台配置 OpenAI 兼容的语音识别服务地址/密钥）
- **回复限速**：同一用户频繁提问自动提示稍候（默认间隔 3 秒，可配置、可热更新）
- **敏感词过滤**：输入可拦截、输出自动打码（`***`），敏感词列表可配置、可热更新
- **插件系统**（`plugins/` 目录）：**无需改源代码**即可扩展功能
  - 单文件 `.py` 或一个文件夹（多文件）都支持；Web 后台一键重新加载、启用/停用（保存后生效，状态持久化）
  - 支持回复型与连接型（桥接）插件：`bot` 上下文可主动发消息/读配置/记日志，`on_start/on_stop` 生命周期钩子
  - 附带示例：示例插件、多文件示例、桥接转发示例
- **Ollama 本地 AI 插件**（`plugins/ollama.py`）：默认用本地 Ollama 回答普通消息，**不消耗 API 额度**；`/` 开头的指令一律放行给主程序/其他插件
- **单文件整合版**（`qqbot_single.py`）：全模块合并单文件，修复打包脚本误删函数体内局部 import 的问题

## 📦 依赖

- Python 3.8+
- `pip install -r requirements.txt`（requests、websocket-client）
- 插件如需调用外部服务（如 Ollama 本地 AI），按插件内说明自行准备

## 🚀 快速开始

```bash
pip install -r requirements.txt
cp config.json.example config.json
# 编辑 config.json：填写 QQ AppID/AppSecret 与 AI 的 api_key/base_url/model
python qqbot.py
```

## 💬 常用指令

| 指令 | 场景 | 说明 |
|---|---|---|
| `/clear` | 私聊 / 群聊 | 清空当前场景的对话历史 |
| `/转移私聊到群聊` | 群聊 | 把私聊上下文合并到当前群（保留群聊原有内容、自动去重） |
| `/生成转移码` | 私聊 | 生成身份绑定码（新码自动作废旧码，10 分钟有效） |
| `/绑定转移码 <码>` | 群聊 | 绑定身份并转移上下文 |

## ⚠️ 注意事项

- `config.json` 含 AppSecret 与 API Key，已加入 `.gitignore`，**切勿提交到公开仓库**
- 群聊被动回复会被 QQ 平台自动 @ 原消息发送者，属官方行为，无法关闭
- `require_mention: false`（回复所有群消息）需开放平台"群消息全量"权限，暂未实现
- 单实例锁按**程序目录**生效：同一份程序目录内防多开；手机与电脑同时运行需手动只保留一个实例
